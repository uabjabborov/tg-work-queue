services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
    env_file:
      - .env
